export enum CellAlign {
  none = 1,
  center = 2,
  centerTop = 3,
  centerBottom = 4,
  leftCenter = 5,
  leftTop = 6,
  leftBottom = 7,
  rightCenter = 8,
  rightTop = 9,
  rightBottom = 10,
}

export enum CellScale {
  none,
  fit,
  fill,
  showAll,
  envelop,
  custom,
}
